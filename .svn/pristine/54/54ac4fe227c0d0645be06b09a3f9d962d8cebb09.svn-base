(function () {
  'use strict';

  angular
    .module('candidates')
    .directive('workExperience', workExperience);

  workExperience.$inject = [/*Example: '$state', '$window' */];

  function workExperience(/*Example: $state, $window */) {
    return {
      template: '<div></div>',
      restrict: 'E',
      link: function postLink(scope, element, attrs) {
       
      }
    };
  }
})();
