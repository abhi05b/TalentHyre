(function () {
  'use strict';

  angular
    .module('candidates')
    .directive('recommendation', recommendation);

  recommendation.$inject = [/*Example: '$state', '$window' */];

  function recommendation(/*Example: $state, $window */) {
    return {
      template: '<div></div>',
      restrict: 'E',
      link: function postLink(scope, element, attrs) {
        
      }
    };
  }
})();
